import React from "react";
import { BidirectionalFlatListProperties } from "./index";

export const BidirectionalFlatList = (_props: BidirectionalFlatListProperties<any>) => {
    throw new Error("bidirectional flat list not supported.");
};
